package me.alpine.bootstrapper;

public class Main {
    public static void main(String[] args) {
        final BootstrapperGui bootstrapperGui = new BootstrapperGui();
        bootstrapperGui.run(args);
    }
}